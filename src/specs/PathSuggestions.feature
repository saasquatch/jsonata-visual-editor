Feature: Suggesting Paths from a JSON Schema source

  When a customer is sending us data, or has configured data, then we can
  suggest paths automatically based on the JSON Schema definitions they have.

  This makes writing examples easier.